<div style="font-size: 35px; text-align: center">
    自然语言处理大作业：传统模型部分
</div>
<div style="font-size: 25px; text-align: center">
    刘炳德 522030910046 王焕宇 522030910212
</div>

---

## 1. 算法实现与结果报告

在 SLU 任务中，我们尝试使用多个基于深度学习的方法完成任务，包括Bert、RoBerta、Seq2Seq等。在本部分，我们将详细介绍我们的模型架构、实验结果等。对于每组实验，我们尽量控制模型的参数量不要过大，以便于在有限的资源下进行训练并进行更加公平的比较。

### 1.1 Baseline

#### 1.1.1 模型架构

<div align=center><img src="./report_images/baseline.png" width="300"></div>

- Embedding: 将 input_ids 编码为词向量
- BiLSTM：得到每个 token 的 hidden state
- Linear Projector：将 hidden state 降维到 number of tags，计算概率得到标注

#### 1.1.2 实验结果

默认超参数设置如下：
| embed_size | hidden_size | lr | dropout | batchsize | LSTM_layer |
| --- | --- | --- | --- | --- | --- |
| 768 | 512 | 1e-3 | 0.2 | 32 | 2 |

实验结果如下：
| 模型大小 | ACC | P Score | R Score | F Score |
| --- | --- | --- | --- | --- |
| 57.85 MB | 71.17 | 80.41 | 73.62 | 76.86 |

### 1.2 Bert & RoBerta

`README_LM.md`中提到了`Bert`和`RoBerta`这两个在自然语言处理领域中非常重要的预训练模型。我们尝试在本部分使用这两个模型来完成 SLU 任务。

#### 1.2.1 模型架构

- Bert 论文：https://arxiv.org/pdf/1810.04805
- 【深度学习】深刻理解BERT模型：https://zhuanlan.zhihu.com/p/11657092508
- RoBerta 论文：https://arxiv.org/pdf/1907.11692
- 详解RoBERTa: A Robustly Optimized BERT Pretraining Approach：https://zhuanlan.zhihu.com/p/593708268

Bert 主要由 Transformer Encoder Block 组成，并对输入进行 Token Embedding、Position Embedding、Segment Embedding 等操作。针对每次输入，Bert 会输出与输入长度相同的向量序列（默认为 768 维），这个向量序列表示了每个输入 Token 的高维信息，我们可以在后续任务中使用这些向量来完成分类、回归等任务。Roberta 模型在 Bert 的基础上进行了一些改进，主要是对训练过程进行了优化，使得模型在训练过程中更加稳定，并未修改模型的架构。

在本次实验中，我们使用了 `Roberta-WWM` 这个预训练模型。

#### 1.2.2 实验结果

我们进行了如下三组实验：

- 实验组 1：在冻结 Roberta 预训练模型的情况下，训练一个 Linear Layer 用于将 Roberta 的输出维度投射到槽值的维度完成预测。
- 实验组 2：类似于（1），但是在训练过程中解冻 Roberta 的参数。
- 实验组 3：使用 Bert 模型的架构，训练一个 Linear Layer 用于将 Bert 的输出维度投射到槽值的维度完成预测，减小模型的参数量，从头开始训练。 

主要超参数如下：（经过少量调参后较好的参数）

优化器：`AdamW, weight_decay=0.001`，`max_epoch = 20`。

| 实验组 | embed_size | hidden_size | lr | dropout | batchsize | num_layer | num_heads | 
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 768 | 768 | 1e-3 | 0.2 | 32 | 6 | 12 |
| 2 | 768 | 768 | 1e-4 | 0.2 | 32 | 6 | 12 |
| 3 | 512 | 768 | 1e-4 | 0.2 | 32 | 2 | 8 |

实验结果：

| 实验组 | 模型大小 | ACC | P Score | R Score | F Score |
| --- | --- | --- | --- | --- | --- |
| 1  | 390.85 MB (不含预训练模型) | 70.50 | 69.99 | 73.20 | 71.56 |
| 2 | 1.14 GB | 76.09 | 79.64 | 79.14 | 79.39 |
| 3 | 182.76MB | 80.78 | 80.65 | 86.17 | 83.32 |

### 1.3 Seq2Seq

#### 1.3.1 模型架构

- Seq2seq论文：https://arxiv.org/abs/1409.3215
- 【神经网络】学习笔记十四——Seq2Seq模型：https://blog.csdn.net/zhuge2017302307/article/details/119979892

<div align=center><img src="./report_images/seq2seq.png" width="600"></div>

自回归的 Encoder-Decoder 架构：
- Encoder：包含 Embedding 和 BiLSTM，获得 input_ids 对应的 hidden state
- MHSA：每次 Decoder 生成时，由当前 token 和所有 Encoder 输出做注意力运算，得到 Context Vector
- Decoder：包含 LSTM 和 Linear Projector
    - LSTM：输入为上一次 decode 过程的结果与当前 Context Vector 的连接
    - Linear Projector：将 hidden state 降维到 number of tags，计算概率得到标注

这样的架构“应当”能够较好综合全序列的信息(得益于 Attention 对 Encoder 输出的加权)与上一次标注的结果(相邻词的标注具有相关性)。

#### 1.3.2 实验结果

优化器设置如下：
```python
optimizer = Adam(grouped_params, lr=args.lr)
scheduler = StepLR(optimizer, step_size=10, gamma=0.6)
```

主要进行以下五组实验，超参数设置：
| 实验组 | embed_size | hidden_size | lr | dropout | batchsize | LSTM_layer | Attention Head |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 768 | 512 | 1e-3 | 0.2 | 32 | 1 | 8 |
| 2 | 768 | 512 | 1e-3 | 0.2 | 32 | 1 | 16 |
| 3 | 768 | 512 | 5e-4 | 0.2 | 32 | 2 | 16 |
| 4 | 768 | 768 | 5e-4 | 0.2 | 32 | 1 | 16 |
| 5 | 768 | 768 | 1e-4 | 0.2 | 32 | 1 | 32 |

实验结果如下：
| 实验组 | 模型大小 | ACC | P Score | R Score | F Score |
| --- | --- | --- | --- | --- | --- |
| 1  | 87.87 MB | 70.61 | 78.06 | 73.83 | 75.88 |
| 2 | 87.87 MB | 71.40 | 81.24 | 74.04 | **77.47** |
| 3 | 105.93 MB | 70.06 | 80.97 | 72.78 | 76.66 |
| 4 | 164.65 MB | 69.94 | 79.68 | 72.78 | 76.08 |
| 5 | 164.65 MB | 69.16 | 77.74 | 71.74 | 74.62 |

### 1.4 Transformer (简化版)

由以上结果可以看出，**1.2** 中的 **(3) Bert** 在保证模型参数量较小的情况下取得了明显优于其他模型的效果。Bert的主要部分是 Transformer Encoder Block。同时，前段时间大火的 Sora 模型，再往前到T2I领域占有很大分量的 Pixart，都采用了 Diffusion Transformer 的架构，Attention机制表现出强大的统治力。因此，我们尝试使用 Transformer 的 Encoder Block 构建一个简化版的模型，用于完成 SLU 任务。另外，Bert中并未采用传统的 `sin/cos` 位置编码，而是使用了更加简单的，让模型自己学习的位置编码，我们在这里将探索这些实现方式。

- Attention is All You Need：https://arxiv.org/abs/1706.03762
- [Sora](https://openai.com/index/video-generation-models-as-world-simulators/)
- OpenSora：https://github.com/hpcaitech/Open-Sora
- Pixart-α：https://arxiv.org/abs/2310.00426

#### 1.4.1 模型架构

模型结构示意图：

```
Input -> Token Embedding -> Positional Embedding (Optional)-> Transformer Encoder Block (× 6) -> Linear Layer -> Output
```

1. 在这里我们没有使用 word2vec-768 中提供的Embedding，一是因为在以往的实验中发现，是否从头学习 Embedding 对模型的影响不大，二是为了减小模型的参数量。
2. Positional Embedding 有三种实现方式：不使用、使用 sin/cos 编码、使用可学习的编码。我们将尝试这三种方式。
3. Transformer Encoder Block 的实现参考了 Bert 的 Encoder Block。

```
# Transformer Encoder Block
x -> MultiHeadAttention -> Add & Norm -> FeedForward -> Add & Norm
```

4. Transformer Encoder Block 始终保持一致的输出维度，因此可以堆叠。最后接一个 Linear Layer 将输出维度投射到槽值的维度完成预测。

#### 1.4.2 实验结果

主要超参数如下：

优化器：`AdamW, weight_decay=0.001`。

```
统一的超参数：embed_size = 512, dropout = 0.1, batchsize = 32, num_heads = 16, max_epoch = 20。
```

| 实验组 | num_blocks | Positional Embedding | lr | 模型大小 | ACC | P Score | R Score | F Score |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2 | 不使用 | 5e-5 | 196.41 MB | 73.63 | 70.26 | 77.05 | 73.50 |
| 2 | 4 | 不使用 | 2e-5 | 268.59 MB | 73.52 | 70.70 | 76.78 | 73.62 |
| 3 | 8 | 不使用 | 5e-5 | 412.96 MB | 72.96 | 70.18 | 75.17 | 72.59 |
| 4 | 2 | sin/cos | 1e-4 | 206.18 MB | 72.40 | 67.78 | 76.51 | 71.88 |
| 5 | 2 | learnable | 2e-4 | 206.18 MB | 74.30 | 73.06 | 78.26 | 75.57 |

## 2. 分析与思考

### 2.1 Attention 机制

在以上的实验中，Bert / Roberta / Transformer（简化版）/ Seq2seq 四个模型使用了 Attention 机制，Baseline 未使用 Attention 机制。从实验结果来看，Attention 机制在 SLU 任务中取得了较好的效果。

Attention 机制相比于传统的 RNN / LSTM 等模型，能够更好地捕捉输入序列中的信息，它不会局限于序列的长度，不会偏向关注序列的前部分或后部分，而是能够根据输入序列的内容自动调整权重。

从传统的 NLP 任务到现代的 Deep Learning + NLP，我们实际上在将更多内容交给模型自己去学习，而不是人工设计特征。Attention 机制是这一思想的体现：相比于人工设计的特征，或者更加精细设计的 RNN / LSTM 等模型，Attention 机制将更多的“裁量权”交给了模型自己，在提高了模型效果的同时也使得模型适用于更多的任务。

### 2.2 模型参数量与预训练模型

- Scaling Laws for Neural Language Models：https://arxiv.org/abs/2001.08361
- Emergent Abilities of Large Language Models：https://arxiv.org/abs/2206.07682

在 **1.2 Bert&RoBerta** 部分的训练中，我们发现使用预训练模型相比于从头训练模型，未必能取得更好的效果。从 **1.4.2** 部分的前三组实验也能看出，增加堆叠的 Transformer Encoder Block 并未能取得更好的效果。

我们推测在SLU任务上，这主要由于我们的数据集较小，模型参数量过大，导致模型过拟合。

在使用预训练模型问题上：Bert / RoBerta 都输出高维的向量序列，它们包含了有关输入序列的丰富信息。我们详细考量 Bert 的训练过程：

Bert 的训练过程是一个自监督的过程，它通过 Masked Language Model 和 Next Sentence Prediction 两个任务来训练模型。
1. Bert 使用 CBOW 的方式来预测 Masked Token，这使得模型能够更好地理解输入序列的上下文信息。
2. 为了更好地理解语义信息，Bert 使用 NSP 任务进行训练。

事实上，这两个自监督学习部分都是针对一个`word token`进行预测的。相应地，Bert 输出的向量所包含的信息也是针对一个`word token`的。在 SLU 任务中，我们的输入是一个句子，输出是相应的槽值序列，`word token`与`slot`二者之间的关系并不是一一对应的。因此，我们认为在 SLU 任务中，使用预训练模型并不一定能取得更好的效果。

举一个极端一些的例子来说：如果我们有一个 `BertForMaskedLM` 的模型，它能够给出当前句子中每个`word token`的概率分布。比如说它的`vocab_size = 20000` ，我们的槽值`num_tags = 1000`，如果我们用一个 Linear Layer 将`vocab_size`维的向量投射到`num_tags`维，并冻结 Bert 的参数，那么这个模型本质上是只使用一个 Linear Layer 来完成 SLU 任务，这样的模型显然是不合理的。

为了应对上面这种情况，我们需要让 Bert 的整体参数（或者至少是后续一部分 Transformer Encoder Block 的参数）参与训练，这样模型才能更好地适应 SLU 任务。但无论如何，这样的模型参数量都会较大，也违背了 Report 中提到的`文件总大小不超过1G`这样的要求。因此，我们认为在 SLU 任务中，使用预训练模型并不一定能取得更好的效果，尤其是在数据集较小的情况下。

### 2.3 Seq2seq 中 Attention 的理解

- 完全解析RNN, Seq2Seq, Attention注意力机制：https://zhuanlan.zhihu.com/p/51383402
- Scheduled Sampling论文：https://arxiv.org/pdf/1506.03099

Seq2seq模型中，我们认为重点应当在于 Context Vector 的获取。

最朴素的做法是将 Encoder 中最后一个 token 的输出作为上下文向量，原因基于其应当包含 BiLSTM 对先前序列的信息融合，这个 Context Vector 将作为每次 Decoder 生成的不变输入。但是这样的设计过于依赖 Encoder 的信息提取能力，有很大的优化空间。

更进一步，我们认为在本任务中，对于单个词的标注主要依赖两个部分：`当前词在整个句子中的词义`以及`前一个词的标注结果`，因此，应在 Decoder 阶段提供这些信息。通过用当前待标注 token 的 Encoder 输出和全序列 Encoder 输出做注意力运算，能够较好加权平均得到当前词的词义；同时将上一次 Decoder 的标注输出也作为输入与 Context Vector 连接，便可以将上述两部分的信息都提供给 Decoder 进行解码。

但是，由于每次 Decoder 的输入都带有前一个 token 的标注结果信息，如果其中某个 token 的标注出错，这样的误差将会在 Decoder 中逐步积累。在我们所使用的训练和测试数据集中，待标注序列长度不长，这样的误差并不会很明显。但是如果涉及到超长序列的标注问题，这样的设计缺陷有待改进。Google 提出 **Scheduled Sampling** 方法，能够较好缓解上述问题。我们也对此进行了测试:

| 实验组 | embed_size | hidden_size | lr | dropout | batchsize | LSTM_layer | Attention Head | With Scheduled Sampling |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 768 | 512 | 1e-3 | 0.2 | 32 | 1 | 16 | No |
| 2 | 768 | 512 | 1e-3 | 0.2 | 32 | 1 | 16 | Yes(0.5) |

实验结果如下：
| 实验组 | 模型大小 | ACC | P Score | R Score | F Score |
| --- | --- | --- | --- | --- | --- |
| 1 | 87.87 MB | 71.40 | 81.24 | 74.04 | **77.47** |
| 2 | 87.87 MB | 75.75 | 78.18 | 76.22 | 77.19 |

可以看出，加入 Scheduled Sampling 后，对于本任务，F Score 提升较小，我们推测是由于本任务较为简单，不涉及长序列的标注，上述误差累积问题较小。
如果出现更困难的任务，Scheduled Sampling 应当能一定程度上提高模型能力。

### 2.4 训练集 / 数据集问题

在简单阅读`train.json`和`development.json`后，我们发现数据集中存在一些不合理的条目，例如下面这个片段：

```json
"utt_id": 2,
"manual_transcript": "到南昌镇",
"asr_1best": "到南昌的",
"semantic": [
    [
        "inform",
        "终点名称",
        "南昌镇"
    ]
]
# 这是在 development.json 中第 964 行的内容
```

在Baseline中，模型读取`asr_1best`字段作为文本输入，并给出每个字对应的`B/I - <act> - <slot>`标签，随后再使用一个函数结合`asr_1best`和对每个字的标签预测生成三元组。我们的模型也使用同样的机制完成训练，同时这样一个机制是符合口语语义理解的 Pipeline 的。但是对于上面这个例子，`semantic`中出现了`asr_1best`中不存在的字，我们的模型并未接受到这样的信息，因此这样建模的模型不可能有能力预测出这样的三元组。

我们使用`scripts/clean.py`去除了`development.json`中这样不合理的条目，并在**1.2 实验组 3**中训练好的 Bert 模型上测试，结果如下：

| ACC | P Score | R Score | F Score |
| --- | --- | --- | --- |
| 83.98 | 85.31 | 87.41 | 86.35 |

可以看到，去除了这样的噪声后，模型的效果有了明显的提升。

相应地，我们研究这样的噪声是否会影响模型的训练效果，在上面的Bert上做了如下实验：

| num_layer | clean | ACC | P Score | R Score | F Score |
| --- | --- | --- | --- | --- | --- |
| 2 | train / development| 84.11 | 82.51 | 89.44 | 85.83 |
| 4 | train / development| 85.50 | 84.41 | 90.88 | 87.53 |
| 6 | train / development| 85.25 | 84.68 | 89.58 | 87.06 |
| 2 | train | 79.89 | 78.70 | 86.31 | 82.33 |

对比此前的训练结果，是否在`train.json`上做过滤对模型的能力影响并不明显，推测这些随机噪声并不会影响模型的训练效果；不过从多次训练尝试（上面四次及之前的大量调参数实验）来看，去除随机噪声之后模型训练的稳定性能够得到提升。当然，这一点依然有待量化验证，考虑到随机数的影响我们并不能下一个结论。

就这个现象而言，我们认为纠正 ASR 的错误文本的责任并不在于 SLU 模型，我们需要提高 ASR 的准确率，或下策是添加一个后处理语言模型来纠正 ASR 的错误（这个后处理模型一定需要大量的语料训练，在这门课程作业中无法实现）。例如

```
我想订一张机票 -> 我想定义张机票
```

这样是一个可以纠正的错误（至少我们人类可以识别出这个谐音错误）。但如果 ASR 发生类似下面的错误

```
我要去南昌镇 -> 我要去南昌站 / 我要去南昌
```

这个错误是不可能被纠正的，错误后的信息仍然是一个合法的句子，这样的错误应当在 ASR 阶段被纠正。

## 3. 任务提交

### 3.1 分工

| 姓名 | 代码部分 | 报告部分 |
| --- | --- | --- |
| 刘炳德 | Bert, Transformer | 1.2, 1.4, 2.1, 2.2, 2.4 |
| 王焕宇 | RoBERTa, Seq2seq | 1.1, 1.3, 2.3 |

### 3.2 文件提交结构

```
data/ (原文件夹，添加了train_new.json和development_new.json，2.4部分提及)
model/ slu_baseline_tagging.py
       slu_roberta_tagging.py
       slu_seq2seq_tagging.py
scripts/ slu_baseline.py
         slu_bert.py
         slu_roberta.py
         slu_seq2seq.py
         slu_transformer.py
         clean.py
utils/ + args_bert.py
       + args_roberta.py
       + args_seq2seq.py
       + args_transformer.py
model.bin
model_bert.bin
model_roberta.bin
model_transformer.bin
model_seq2seq.bin
word2vec-768.txt
```