# Report of LLM on Spoken Language Understanding (SLU)

Huanyu Wang 522030910212

## 模型选取

文心一言 V.S. ChatGPT

## One-Shot Learning

### Prompt

使用新的对话，给出一个语义理解分析的例子，然后要求模型分析另一个例子：

```
"manual_transcript": "导航到安德第六地税所(robot)前方六百米有限速拍照限速一百",
            "asr_1best": "导航到安德第六地税所拍照",
            "semantic": [
                [
                    "inform",
                    "操作",
                    "导航"
                ],
                [
                    "inform",
                    "终点名称",
                    "安德第六地税所"
                ]
            ]
仿照上述例子，帮我分析：
"manual_transcript": "你可以睡觉了",
            "asr_1best": "你可以睡觉了",
            "semantic": []
```

### Result

- 文心一言
```
对于提供的例子，我们可以进行以下分析：

manual_transcript: "你可以睡觉了"
这句话是一个简单的陈述或建议，表示对方可以或应该去睡觉了。

asr_1best: "你可以睡觉了"
这里，自动语音识别（ASR）的输出与原始的手动转录（manual_transcript）完全一致，说明ASR在这句话上的识别准确率很高。

semantic: []
在语义分析部分，给出的列表是空的。这可能是因为这句话并没有包含太多可以提取为结构化信息的语义内容。它主要是一个表达建议或陈述的句子，没有明确的“操作”、“目的地”、“时间”等可以明确归类为语义信息的元素。

然而，如果我们尝试对这句话进行更细致的语义分析，可能会提取出一些隐含的信息，比如：

情感/意图：这句话可能表达了一种关心或建议的意图，即建议对方去休息。
对象：虽然句子没有明确指出对象，但我们可以推断出这句话是对某人说的。
但在提供的“semantic”格式中，这些信息可能并不容易被明确地表示出来，因为它们并不直接对应于“操作”、“终点名称”等具体的语义类别。因此，在这种情况下，语义分析的结果可能就是一个空列表。

总结来说，这句话的ASR识别准确率很高，但在语义分析方面，由于句子本身并没有包含太多可以明确提取为结构化信息的元素，所以语义分析的结果是一个空列表。
```

- ChatGPT
```
根据给定的例子和分析方法，以下是对 `"manual_transcript": "你可以睡觉了"` 的分析：  

"manual_transcript": "你可以睡觉了",
"asr_1best": "你可以睡觉了",
"semantic": [
    [
        "inform",
        "动作",
        "睡觉"
    ]
]

**分析说明：**
1. **`manual_transcript` 和 `asr_1best` 对应一致**：这表明语音识别准确地捕捉了用户的语音输入。
2. **语义结构 `semantic`**：  
   - `"inform"` 表示传递信息的意图。  
   - `"动作"` 是语义槽位，用于描述行为类信息。  
   - `"睡觉"` 是该槽位的值，具体表示该动作为“睡觉”。  

如有其他上下文或更具体的需求，可进一步调整语义标签和结构。
```

### Conclusion

很明显，文心一言并没有理解我的意图。ta分析了我给出的整个例子，没有补全其中的semantic部分，而是将其理解为本身就是空列表。但是，ChatGPT能够理解并帮我补全semantic部分，尽管可能缺少对“你”的语义分析。

**注意**: Prompt中的行首多个Tab空格是由于从json文件中直接多行复制粘贴导致，此时我并没有处理这些Tab。但是事实上，从后续Prompt Engineering中我发现，只需要删除行首的多个Tab，其余部分原封不动，文心一言就能够准确识别我的意图，并给出相对正确的回答。我认为，这很有可能是文心一言对于json的识别能力较弱，无法识别到缩进不规范的用户输入json，从而导致无法理解我想要补全json中semantic部分的潜在意图。

## In-Context Learning

### Prompt

在上述对话中继续进行：

```
"manual_transcript": "把导航退掉",
            "asr_1best": "把导航退掉",
            "semantic": [
                [
                    "inform",
                    "操作",
                    "退掉"
                ],
                [
                    "inform",
                    "对象",
                    "导航"
                ]
            ]
这是同样任务的另一个例子，请仿照这个例子和刚才我给出的例子，帮我分析：
"manual_transcript": "去你家",
            "asr_1best": "去你家",
            "semantic": []
```

### Result

- 文心一言
```
对于提供的这个新例子，我们可以进行以下分析：

manual_transcript: "去你家"
这句话是一个简单的陈述或指令，表示要前往对方的家。

asr_1best: "去你家"
这里，自动语音识别（ASR）的输出与原始的手动转录（manual_transcript）完全一致，说明ASR在这句话上的识别准确率很高。

semantic: []
在语义分析部分，给出的列表是空的。这可能是因为这句话虽然简洁，但包含了足够明确的行动指令，即“去你家”，然而这个指令在提供的“semantic”格式中并没有直接对应的类别进行明确归类。

然而，如果我们尝试对这句话进行语义上的解读，可以提取出以下信息：

操作：去
目的地：你家
但在给出的“semantic”示例格式中，这些信息虽然可以被理解为“操作”和“目的地”的类别，但示例中并没有明确将这些信息以结构化的方式表达出来。可能是因为这个格式更适用于包含多个具体语义元素（如操作类型、目的地名称、时间等）的复杂句子，而对于这种简洁明了的句子，可能并不需要如此详细的语义分析。

总结来说，这句话的ASR识别准确率很高，但在提供的“semantic”格式中，由于句子本身的简洁性和直接性，并没有进行详细的语义分析，因此语义分析的结果是一个空列表。然而，从语义解读的角度来看，这句话包含了明确的“操作”和“目的地”信息。
```

- ChatGPT
```
以下是对 `"manual_transcript": "去你家"` 的分析：  

"manual_transcript": "去你家",
"asr_1best": "去你家",
"semantic": [
    [
        "inform",
        "操作",
        "去"
    ],
    [
        "inform",
        "目标",
        "你家"
    ]
]

**分析说明：**
1. **`manual_transcript` 和 `asr_1best` 一致**：语音识别准确。
2. **语义结构 `semantic`**：  
   - 第一条语义信息表示动作是“去”，因此槽位为 `"操作"`，值为 `"去"`。
   - 第二条语义信息表示目标是“你家”，因此槽位为 `"目标"`，值为 `"你家"`。

这种分解方式符合例子中的语义分析结构，可以准确表达用户意图。
```

### Conclusion

文心一言依然不能理解我的意图，而ChatGPT仍然给出了令人满意的回复。

## Prompt Engineering

在上述实验中，文心一言并没有理解语义分析的要求和期望得到的答案。因此，在新的会话中，尝试进行prompt修改。

### Prompt

原封不动复制Few-Shot Learning中的Prompt，但是将json缩进规范化，删除行首的Tab空格：

```
"manual_transcript": "导航到安德第六地税所(robot)前方六百米有限速拍照限速一百",
"asr_1best": "导航到安德第六地税所拍照",
"semantic": [
    [
        "inform",
        "操作",
        "导航"
    ],
    [
        "inform",
        "终点名称",
        "安德第六地税所"
    ]
]
仿照上述例子，帮我分析：
"manual_transcript": "你可以睡觉了",
"asr_1best": "你可以睡觉了",
"semantic": []
```

### Result

- 文心一言
```
{
    "manual_transcript": "你可以睡觉了",
    "asr_1best": "你可以睡觉了",
    "semantic": [
        [
            "instruct",
            "动作",
            "睡觉"
        ],
        [
            "instruct",
            "对象",
            "你"
        ]
    ]
}
在这个分析中，我尝试将句子的语义信息以一种结构化的方式提取出来。

instruct：这个标签表示这是一个指令或建议。由于句子“你可以睡觉了”是在给出一种建议或允许，所以使用了这个标签。
动作：这里指的是句子中建议或要求执行的具体动作，即“睡觉”。
对象：这里指的是动作的执行者，即“你”。虽然“你”在中文句子中通常是隐含的，但在这里为了明确语义，我还是将其提取出来。
注意，这种语义分析的方式并不是唯一的，也可能存在其他合理的分析方式。我的分析主要是基于句子本身和常见的语义理解模式进行的。
```

### Conclusion

神奇的是，此时文心一言就能够给出和ChatGPT不相上下的回复。由于我一直只使用ChatGPT辅助编程，从代码中直接复制时不可避免会有行首缩进不统一的情况，但是ChatGPT不会受此影响(至少没有让我感受到回答质量下降)。上述Few-Shot Learning实验中证明，这其实是ChatGPT的优势，能够应对更加灵活的用户输入json或代码的格式。我必须给文心一言提供规范的json格式的Prompt，才能得到准确的回答。

## Zero-Shot Learning

### Prompt

在新的对话中，不提供例子，但是提供较为详细的任务描述，测试模型Zero-Shot Learning能力(缩进已经规范化)：

```
"manual_transcript": "附近火锅店在什么位置",
"asr_1best": "附近火锅店在什么位置",
"semantic": []
我想为上述例子进行语义理解分析，请补全semantic部分，其中包含一或多个语义单元，每个语义单元由动作（act），语义槽（slot），槽值（value）三部分组成
```

### Result

- 文心一言

```
对于给定的例子，我们可以进行如下的语义理解分析，并补全semantic部分：

"semantic": [
    {
        "act": "查询位置",
        "slot": "查询对象",
        "value": "火锅店"
    },
    {
        "act": "查询位置",
        "slot": "地理位置",
        "value": "附近"
    }
]
在这个分析中，我们定义了两个语义单元：

第一个语义单元表示的是一个“查询位置”的动作，其中“查询对象”是“火锅店”，这表示用户想要查询的是火锅店的位置。
第二个语义单元同样表示的是一个“查询位置”的动作，但这次关注的是地理位置，即“附近”，这表示用户想要查询的是他/她附近位置的火锅店。
这样的语义理解分析有助于机器更好地理解用户的查询意图，并给出相应的答复或执行相应的操作。
```

- ChatGPT

```
对于上述例子，语义理解分析可以分解为以下语义单元：  

"semantic": [
    {
        "act": "query_location",
        "slot": "place_type",
        "value": "火锅店"
    },
    {
        "act": "query_location",
        "slot": "proximity",
        "value": "附近"
    }
]

解释：  
- 动作（`act`）为查询地点（`query_location`），表示用户想要获取某个地方的位置。  
- 语义槽（`slot`）包含两个维度：
  - `place_type`表示用户感兴趣的地点类型（这里是“火锅店”）。  
  - `proximity`表示距离范围（这里是“附近”）。  
- 槽值（`value`）对应用户提到的具体信息，如“火锅店”和“附近”。
```

### Conclusion

在这个设定下，文心一言和ChatGPT都给出了正确的回答，而且格式上符合Prompt中定义的语义单元组成。

## Analysis and Discussion

1. 实验结果分析

在本次任务中，我们使用了两种生成式大模型：文心一言和 ChatGPT，针对口语语义理解进行了对比分析。实验采用了 few-shot、in-context learning、prompt-engineering 和 one-shot 四种setting，以全面探究两种大模型在不同场景下的表现。

从实验结果来看，两种模型在不同设置下的表现各有特点：

- Few-shot Learning
在没有规范json缩进的Prompt下，文心一言并没有理解我的意图，转而分析了我的Prompt本身的语义。但是，ChatGPT能够理解并帮我补全待分析的json部分中的semantic，尽管可能缺少对“你”的语义分析。在给出一个例子后，chatgpt能够较好完成语义理解的任务。

- In-context Learning
在同一个对话但仍是没有规范json缩进的Prompt下，文心一言仍然没有理解任务要求，ChatGPT仍然能够较好完成任务。

- Prompt Engineering
事实上，只需要将Prompt中的json部分处理成规范的缩进，删除行首的多个Tab空格，就能够让文心一言理解我的任务。由此可以看出，文心一言在应对灵活多变的用户输入时，能力逊色于ChatGPT，在Prompt中需要提供规范的json或代码格式，文心一言才能做出准确的回答。

- Zero-shot Learning
不给出例子，但是对任务提供更详细的要求，两个模型都能够很好得完成语义理解分析的任务，并且回答的格式上遵守Prompt中给出的要求。

以上实验setting中，需要分析的语言较为简单，因此在zero-shot下两个模型都能够给出优质回答。唯一的问题就是文心一言对于不规范的代码或json输入，无法准确分割Prompt中的例子和任务要求，导致回答出错。

2. 大模型在口语语义理解中的现状

生成式大模型的兴起为口语语义理解任务带来了巨大的技术红利，其强大的语言生成与理解能力，显著降低了传统自然语言处理任务中的数据依赖。并且，大模型不需要进行微调就能够取得较好的效果，同时能够继承大模型本身拥有的优势，如知识，拒绝回答，工具使用等，使得大模型对口语语义理解任务有非常好的能力，能够处理噪声更多的文本。

然而，目前的技术应用仍存在一些不足：

对复杂语义的理解能力有限：
口语通常包含大量语气词、省略表达、文化背景及上下文关联，这对模型的综合语义理解能力提出了较高要求。同时，如果缺少必要背景知识和上下文，大模型或许无法给出精确的语义分析。

语言生成质量差异：
不同模型生成的语义解析结果在流畅度、逻辑性与细节完备性上表现不一，需要在实际应用中针对性地选择最优模型。

3. 未来研究方向

为了进一步提升生成式大模型在口语语义理解中的应用效果，未来的研究可以从以下几个方面入手：

提升对语境的动态适应能力：
当前大模型对上下文的利用能力虽然显著，但在冗长对话或模糊语义场景中，仍难以保证解析的连贯性。未来研究可以通过引入动态记忆机制或多模态信息融合，进一步提高模型的语境感知能力。

开发领域特化模型：
针对特定任务或行业，研发更加精细化的大模型，通过少量领域数据微调模型参数，提升其在特定语义场景中的适用性。

多语言及跨文化语义理解：
大模型在跨语言与跨文化语义理解任务中仍有较大提升空间。研究方向可包括引入多语言协同训练机制，或通过知识蒸馏技术加强模型在不同文化背景下的迁移学习能力。

优化 Prompt Design 方法：
Prompt 的设计直接影响生成结果，然而目前缺乏系统化的 Prompt 优化策略。未来研究可以借助自动化 Prompt 搜索或生成技术，探索通用或领域适应性的 Prompt 生成框架。

改进模型评估指标：
口语语义理解任务需要更精细化的评估指标，以量化模型在复杂语义场景中的表现，例如语义准确性、上下文连贯性及文化背景适应性等。

注重用户体验：
在实际应用中，大模型的响应速度、交互能力和解释性对用户体验至关重要。未来研究可以进一步优化模型架构，提升实时性与用户友好性。

4. 结论

本次实验的结果表明，生成式大模型在口语语义理解任务中已经表现出较强的能力，但仍有一定提升空间。文心一言与 ChatGPT 各具优势，适用于不同的语义任务场景。在未来，大模型技术的发展将继续推动口语语义理解的广泛应用，而通过针对性优化与技术创新，进一步克服现有挑战，实现更高效、更智能的人机交互。

5. 参考文献

[Awesome ChatGPT Prompts](https://github.com/f/awesome-chatgpt-prompts)

[Prompt Engineering Guide](https://github.com/dair-ai/Prompt-Engineering-Guide)

OpenAI (2023). "GPT-4 Technical Report." Retrieved from https://openai.com.

Baidu (2023). "文心一言: AI生成式模型的技术白皮书." Retrieved from https://wenxin.baidu.com.